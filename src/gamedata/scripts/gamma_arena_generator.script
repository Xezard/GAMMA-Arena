function generate(session, fight_index, catalog, layout)
    return gamma_arena_fight_builder.generate(session, fight_index, catalog, layout)
end

function stable_encode(spec)
    return gamma_arena_fight_spec.stable_encode(spec)
end
